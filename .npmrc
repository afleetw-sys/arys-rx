# Use hoisted node_modules layout so Metro bundler can resolve all transitive deps
node-linker=hoisted

# Don't fail on peer dependency conflicts (React Native ecosystem has many)
strict-peer-dependencies=false

# Avoid lockfile issues in CI
prefer-frozen-lockfile=false
